apiVersion: v1
kind: Service
metadata:
  name: kube-game-operator-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/instance: kube-game-operator
    app.kubernetes.io/name: kube-game-operator