apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
  labels:
    app.kubernetes.io/instance: asyncunitytemplate
    app.kubernetes.io/name: core
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll      
    keel.sh/pollSchedule: "@every 1m"
spec:
  revisionHistoryLimit: 1
  {{- if not .Values.image.autoscaling.enabled }}
  replicas: {{ .Values.image.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: asyncunitytemplate
      app.kubernetes.io/name: core
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: asyncunitytemplate
        app.kubernetes.io/name: core
    spec:
      serviceAccountName: {{ include "helm.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
          - name: INSTANCE_IMAGE
            value: "{{ .Values.instance.repository }}:{{ .Values.instance.tag }}"
          - name: KUBERNETES_NAMESPACE
            value: "{{ .Values.namespace }}"
          - name: KUBERNETES_INGRESS_NAME
            value: "{{ .Values.ingress.name }}"
          - name: KUBERNETES_SERVICE_NAME
            value: core
          - name: KUBERNETES_SERVICE_PORT
            value: "{{ .Values.service.port }}"
          - name: KUBERNETES_PORT
            value: "{{ .Values.instance.externalPort }}"
          - name: BACKEND_URL
            {{- if and .Values.httpRoute.enabled (gt (len .Values.httpRoute.hosts) 0) }}
            value: {{ (index .Values.httpRoute.hosts 0).host }}
            {{- else }}
            {{- with (index .Values.ingress.hosts 0) }}
            value: {{ .host }}
            {{- end }}
            {{- end }}
          - name: HTTP_ROUTE_ENABLED
            value: "{{ .Values.httpRoute.enabled }}"
          - name: GATEWAY_NAME
            value: "{{ .Values.httpRoute.gatewayName }}"
          - name: GATEWAY_NAMESPACE
            value: "{{ .Values.httpRoute.gatewayNamespace }}"
          - name: PASSWORD
            value: {{ .Values.image.password }}
          - name: REDIS_URL
            value: "redis.{{ .Values.namespace }}.svc.cluster.local"
          - name: REDIS_PORT
            value: "{{ .Values.redis.port }}"
          - name: EXTENSION
            value: {{ .Values.image.extension }}
          - name: EXTERNAL_PORT
            value: "{{ .Values.instance.externalPort }}"
          - name: INTERNAL_PORT
            value: "{{ .Values.instance.internalPort }}"
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /test?string=test&number=5263
              port: http
          readinessProbe:
            httpGet:
              path: /test?string=test&number=5263
              port: http
          volumeMounts:
          - name: kubernetes-config
            mountPath: "/src/kubernetes-config"
            subPath: config
            readOnly: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
      - name: kubernetes-config
        configMap:
        # Provide the name of the ConfigMap you want to mount.
          name: kubernetes-config
          items:
          - key: config
            path: config
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app.kubernetes.io/instance: asyncunitytemplate
    app.kubernetes.io/name: frontend
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll      
    keel.sh/pollSchedule: "@every 1m"
spec:
  revisionHistoryLimit: 1
  {{- if not .Values.frontend.autoscaling.enabled }}
  replicas: {{ .Values.frontend.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: asyncunitytemplate
      app.kubernetes.io/name: frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: asyncunitytemplate
        app.kubernetes.io/name: frontend
    spec:
      serviceAccountName: {{ include "helm.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.frontend.repository }}:{{ .Values.frontend.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.frontend.pullPolicy }}
          env:
          - name: BACKEND_URL
            {{- if and .Values.httpRoute.enabled (gt (len .Values.httpRoute.hosts) 0) }}
            value: {{ (index .Values.httpRoute.hosts 0).host }}
            {{- else }}
            {{- with (index .Values.ingress.hosts 0) }}
            value: {{ .host }}
            {{- end }}
            {{- end }}
          - name: BACKEND_PROTOCOL
            value: {{ .Values.frontend.backendProtocol | default "https" }}
          - name: HTTP_ROUTE_ENABLED
            value: "{{ .Values.httpRoute.enabled }}"
          ports:
            - name: http
              containerPort: {{ .Values.frontend.port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
{{- if .Values.instance.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: instance-fixe
  labels:
    app.kubernetes.io/instance: asyncunitytemplate
    app.kubernetes.io/name: instance-fixe
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll      
    keel.sh/pollSchedule: "@every 1m"
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: asyncunitytemplate
      app.kubernetes.io/name: instance-fixe
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: asyncunitytemplate
        app.kubernetes.io/name: instance-fixe
    spec:
      serviceAccountName: {{ include "helm.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: instance-fixe
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.instance.repository }}:{{ .Values.instance.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
          - name: INSTANCE_IMAGE
            value: "{{ .Values.instance.repository }}:{{ .Values.instance.tag }}"
          - name: KUBERNETES_NAMESPACE
            value: "{{ .Values.namespace }}"
          - name: KUBERNETES_INGRESS_NAME
            value: "{{ .Values.ingress.name }}"
          - name: KUBERNETES_SERVICE_NAME
            value: core
          - name: KUBERNETES_SERVICE_PORT
            value: "{{ .Values.service.port }}"
          - name: KUBERNETES_PORT
            value: "{{ .Values.service.port }}"
          - name: BACKEND_URL
            {{- if and .Values.httpRoute.enabled (gt (len .Values.httpRoute.hostnames) 0) }}
            value: {{ index .Values.httpRoute.hostnames 0 }}
            {{- else }}
            {{- with (index .Values.ingress.hosts 0) }}
            value: {{ .host }}
            {{- end }}
            {{- end }}
          - name: HTTP_ROUTE_ENABLED
            value: "{{ .Values.httpRoute.enabled }}"
          - name: GATEWAY_NAME
            value: "{{ .Values.httpRoute.gatewayName }}"
          - name: GATEWAY_NAMESPACE
            value: "{{ .Values.httpRoute.gatewayNamespace }}"
          - name: GATEWAY_HOSTNAMES
            value: "{{ join "," .Values.httpRoute.hostnames }}"
          - name: PASSWORD
            value: {{ .Values.image.password }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          volumeMounts:
          - name: kubernetes-config
            mountPath: "/src/kubernetes-config"
            subPath: config
            readOnly: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
      - name: kubernetes-config
        configMap:
        # Provide the name of the ConfigMap you want to mount.
          name: kubernetes-config
          items:
          - key: config
            path: config
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}